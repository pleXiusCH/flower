import { gdAddAndLog, gdAddAndLogNodeImpls } from '@flower/node-impls'

export const endToEndTest = () => {
  console.log(`GraphDefinition: \n${JSON.stringify(gdAddAndLog)}`)
  console.log(`GraphDefinition: \n${JSON.stringify(gdAddAndLog)}`)
}
