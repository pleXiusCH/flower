{
  "name": "@plexius/flower-core",
  "version": "0.0.1",
  "description": "flower-core",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -b tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "jest",
    "test:cover": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "author": {
    "name": "Peter Unger",
    "email": "peter@pexius.ch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pleXiusCH/flower",
    "directory": "packages/flower-core"
  },
  "license": "MIT",
  "devDependencies": {
    "@plexius/flower-nodes": "^1.0.0",
    "@types/jest": "^26.0.20",
    "@types/uuid": "^8.0.0",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@plexius/flower-interfaces": "^1.0.0",
    "uuid": "^8.3.2"
  },
  "peerDependencies": {
    "rxjs": "^6.5.3"
  }
}
